# Copy file này thành .env và điền giá trị thật trước khi chạy production
# File .env KHÔNG được commit lên git

# JWT secret — bắt buộc trong production (tối thiểu 256-bit, base64-encoded)
# Tạo bằng: openssl rand -base64 32
JWT_SECRET=replace-with-a-strong-random-secret-at-least-32-chars
